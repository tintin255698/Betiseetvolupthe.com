{% extends 'base.html.twig' %}

{%  block title %}Modification du profil utilisateur{% endblock %}

{% block body %}

    {% include 'navbar/index2.html.twig' %}

    <!-- ======= Breadcrumbs Section ======= -->
    <section class="breadcrumbs">
        <div class="container">

            <div class="d-flex justify-content-between align-items-center">
                <h2>Votre panier</h2>
                <ol>
                    <li><a href="{{ path('index')}}#hero">Acceuil</a></li>
                    <li>Votre panier</li>
                </ol>
            </div>
        </div>
    </section><!-- End Breadcrumbs Section -->
    <section class="inner-page">
    <div class="container">

    <div class="container">
        <h1 class="titre mb-5">Modification du profil</h1>
        {{ form_start(user) }}
        {{ form_widget(user)}}

        <button type="submit" class="btn btn-primary mb-5">Enregistrer les modifications</button>

        {{ form_end(user) }}

    </div>

{% endblock %}