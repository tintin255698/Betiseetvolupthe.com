{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
    {% include 'navbar/index2.html.twig' %}

    <main id="main">

        <!-- ======= Breadcrumbs Section ======= -->
        <section class="breadcrumbs">
            <div class="container">

                <div class="d-flex justify-content-between align-items-center">
                    <h2>Adresse</h2>
                    <ol>
                        <li><a href="{{ path('index')}}#hero">Accueil</a></li>
                        <li>Adresse</li>
                    </ol>
                </div>

            </div>
        </section><!-- End Breadcrumbs Section -->

        <section class="inner-page">
            <div class="container">

                <div class="mb-5">

                    {% include 'menu_admin.html.twig' %}
                    <h1 class="mt-4 mb-4 text-center">Liste des adresses (supprimer apres chaque commande)</h1>
                    <table id="admin" class="table table-striped table-bordered" style="width:100%">
                        <thead>
                        <tr>
                            <td>Nom</td>
                            <td>Prenom</td>
                            <td>Societe</td>
                            <td>Adresse</td>
                            <td>Adresse 2</td>
                            <td>Code postal</td>
                            <td>Ville</td>
                            <td>Suppression</td>
                        </tr>
                        </thead>
                        <tbody>
                        {% for plas in pla %}
                        <tr>
                            <td>{{ plas.nom }}</td>
                            <td>{{ plas.prenom }}</td>
                            <td>{{ plas.societe }}</td>
                            <td>{{ plas.adresse }}</td>
                            <td>{{ plas.adresse2 }}</td>
                            <td>{{ plas.cp }}</td>
                            <td> {{ plas.ville }}</td>
                            <td><a href="{{ path('supprimer_commande', {'id':plas.id}) }}" class="btn btn-dark"><i class='bx bx-check-double'></i></a></td>
                            {% endfor %}
</tr>
                    </table>

                </div>
            </div>

        </section>


    </main><!-- End #main -->

{% endblock %}