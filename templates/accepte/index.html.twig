{% extends 'base.html.twig' %}

{% block title %}Ajout commentaire{% endblock %}

{% block stylesheets %}
    <link href="/css/footerInner.css" rel="stylesheet">
{% endblock %}

{% block body %}


    <main id="main">

        {% include 'navbar/index2.html.twig' %}

        <!-- ======= Breadcrumbs Section ======= -->
        <section class="breadcrumbs">
            <div class="container">



            <div class="d-flex justify-content-between align-items-center">
                <h2>Votre adresse de livraison</h2>
                <ol>
                    <li><a href="{{ path('index')}}#hero">Accueil</a></li>
                    <li>Votre adresse de livraison</li>
                </ol>
            </div>
        </div>
    </section><!-- End Breadcrumbs Section -->
    <section class="inner-page">
        <div class="container">
            <div class="class mt-5">
            <h3 class="mt-5">Progression de la commande :</h3>
            </div>



            <div class="progress">
                <div class="progress-bar" role="progressbar" aria-valuenow="80" aria-valuemin="0" aria-valuemax="100">80</div>
            </div>

            <div class="class mt-5 mb-5">
        <h1 class="text-center mb-5">Enregistrement de votre adresse de livraison</h1>
            </div>

        <p>Si vous souhaitez venir chercher votre commande chez nous, merci de saisir l'adresse du restaurant : </p>
        <ul>
            <li>Votre nom et prénom</li>
            <li>79 Rue des Granges</li>
            <li>25000 Besançon</li>
        </ul>

<div class="mt-5">
        {{ form(form) }}
    <div class="text-right">
        <h2 class="text-center text-info">Merci de commander la veille pour le lendemain au minimum</h2>
    <button class="btn mt-3" type="submit">Payer - {{ total }}€ - <small> With Stripe <i class='bx bxl-stripe' ></i></small></button>
        <button type="submit" class="btn mt-3">Réserver</button>

</div>
    </div>
        </div>
    </section>
</main>
{% endblock %}


    {% block javascripts %}
        <script>

            var delay = 500;
            $(".progress-bar").each(function(i){
                $(this).delay( delay*i ).animate( { width: $(this).attr('aria-valuenow') + '%' }, delay );

                $(this).prop('Counter',0).animate({
                    Counter: $(this).text()
                }, {
                    duration: delay,
                    easing: 'swing',
                    step: function (now) {
                        $(this).text(Math.ceil(now)+'%');
                    }
                });
            });

        </script>

    {% endblock %}

