{% extends 'base.html.twig' %}

{% block title %}Avis des clients{% endblock %}

{% block body %}
<div class="container">


    <h2 class="titre pb-3">Avis des clients</h2>
    {% for repos in repo %}
            <div class="bg-light rounded mb-5 py-3 px-3">
                <strong>{{ repos.prenom }} {{ repos.nom }}</strong> a dit :
                <blockquote>
                    {{ repos.contenu }}
                </blockquote>
                <strong>Note donnée par {{ repos.prenom }} {{ repos.nom }}</strong> :
                <span class="note">
        {% for i in 1..5 %}
            {% if i <= repos.note %}
                <i class="icofont-star"></i>
            {% else  %}
                <i class="icofont-star-shape"></i>
            {% endif %}
        {% endfor %}
</span>
            </div>
        {% endfor%}



</div>











{% endblock %}
